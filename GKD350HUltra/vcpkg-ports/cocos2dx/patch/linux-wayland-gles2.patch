diff --git a/cocos/2d/CCClippingNode.cpp b/cocos/2d/CCClippingNode.cpp
index 69fbfbb..d5fa2b9 100644
--- a/cocos/2d/CCClippingNode.cpp
+++ b/cocos/2d/CCClippingNode.cpp
@@ -35,7 +35,8 @@
 #include "base/CCDirector.h"
 #include "base/CCStencilStateManager.h"
 
-#if (CC_TARGET_PLATFORM == CC_PLATFORM_MAC || CC_TARGET_PLATFORM == CC_PLATFORM_WIN32 || CC_TARGET_PLATFORM == CC_PLATFORM_LINUX)
+#if (CC_TARGET_PLATFORM == CC_PLATFORM_MAC || CC_TARGET_PLATFORM == CC_PLATFORM_WIN32 || \
+    (CC_TARGET_PLATFORM == CC_PLATFORM_LINUX && !defined(ROCGALGAME_LINUX_GLES2)))
 #define CC_CLIPPING_NODE_OPENGLES 0
 #else
 #define CC_CLIPPING_NODE_OPENGLES 1
diff --git a/cocos/3d/CCTerrain.cpp b/cocos/3d/CCTerrain.cpp
index 19335b2..fcda460 100644
--- a/cocos/3d/CCTerrain.cpp
+++ b/cocos/3d/CCTerrain.cpp
@@ -138,7 +138,8 @@ void Terrain::onDraw(const Mat4 &transform, uint32_t /*flags*/)
 
     auto glProgram = getGLProgram();
     glProgram->use();
-#if (CC_TARGET_PLATFORM == CC_PLATFORM_MAC) || (CC_TARGET_PLATFORM == CC_PLATFORM_WIN32) || (CC_TARGET_PLATFORM == CC_PLATFORM_LINUX)
+#if (CC_TARGET_PLATFORM == CC_PLATFORM_MAC) || (CC_TARGET_PLATFORM == CC_PLATFORM_WIN32) || \
+    ((CC_TARGET_PLATFORM == CC_PLATFORM_LINUX) && !defined(ROCGALGAME_LINUX_GLES2))
     if(_isDrawWire)
     {
         glPolygonMode(GL_FRONT_AND_BACK,GL_LINE);
@@ -216,7 +217,8 @@ void Terrain::onDraw(const Mat4 &transform, uint32_t /*flags*/)
     }
     glActiveTexture(GL_TEXTURE0);
 
-#if (CC_TARGET_PLATFORM == CC_PLATFORM_MAC) || (CC_TARGET_PLATFORM == CC_PLATFORM_WIN32) || (CC_TARGET_PLATFORM == CC_PLATFORM_LINUX)
+#if (CC_TARGET_PLATFORM == CC_PLATFORM_MAC) || (CC_TARGET_PLATFORM == CC_PLATFORM_WIN32) || \
+    ((CC_TARGET_PLATFORM == CC_PLATFORM_LINUX) && !defined(ROCGALGAME_LINUX_GLES2))
     if(_isDrawWire)//reset state.
     {
         glPolygonMode(GL_FRONT_AND_BACK,GL_FILL);
diff --git a/cocos/base/CCConfiguration.cpp b/cocos/base/CCConfiguration.cpp
index 28ad261..9843146 100644
--- a/cocos/base/CCConfiguration.cpp
+++ b/cocos/base/CCConfiguration.cpp
@@ -113,11 +113,16 @@ std::string Configuration::getInfo() const
 
 void Configuration::gatherGPUInfo()
 {
-	_valueDict["gl.vendor"] = Value((const char*)glGetString(GL_VENDOR));
-	_valueDict["gl.renderer"] = Value((const char*)glGetString(GL_RENDERER));
-	_valueDict["gl.version"] = Value((const char*)glGetString(GL_VERSION));
+    const char* glVendor = (const char*)glGetString(GL_VENDOR);
+    const char* glRenderer = (const char*)glGetString(GL_RENDERER);
+    const char* glVersion = (const char*)glGetString(GL_VERSION);
+    const char* glExtensions = (const char*)glGetString(GL_EXTENSIONS);
 
-    _glExtensions = (char *)glGetString(GL_EXTENSIONS);
+	_valueDict["gl.vendor"] = Value(glVendor ? glVendor : "unknown");
+	_valueDict["gl.renderer"] = Value(glRenderer ? glRenderer : "unknown");
+	_valueDict["gl.version"] = Value(glVersion ? glVersion : "unknown");
+
+    _glExtensions = (char *)(glExtensions ? glExtensions : "");
 
     glGetIntegerv(GL_MAX_TEXTURE_SIZE, &_maxTextureSize);
 	_valueDict["gl.max_texture_size"] = Value((int)_maxTextureSize);
diff --git a/cocos/base/CCStencilStateManager.cpp b/cocos/base/CCStencilStateManager.cpp
index 2747fed..6e87e0d 100644
--- a/cocos/base/CCStencilStateManager.cpp
+++ b/cocos/base/CCStencilStateManager.cpp
@@ -30,7 +30,8 @@
 #include "renderer/ccGLStateCache.h"
 #include "renderer/CCRenderer.h"
 #include "renderer/CCRenderState.h"
-#if (CC_TARGET_PLATFORM == CC_PLATFORM_MAC || CC_TARGET_PLATFORM == CC_PLATFORM_WIN32 || CC_TARGET_PLATFORM == CC_PLATFORM_LINUX)
+#if (CC_TARGET_PLATFORM == CC_PLATFORM_MAC || CC_TARGET_PLATFORM == CC_PLATFORM_WIN32 || \
+    (CC_TARGET_PLATFORM == CC_PLATFORM_LINUX && !defined(ROCGALGAME_LINUX_GLES2)))
 #define CC_CLIPPING_NODE_OPENGLES 0
 #else
 #define CC_CLIPPING_NODE_OPENGLES 1
diff --git a/cocos/platform/desktop/CCGLViewImpl-desktop.cpp b/cocos/platform/desktop/CCGLViewImpl-desktop.cpp
index 74e2ca7..edd353e 100644
--- a/cocos/platform/desktop/CCGLViewImpl-desktop.cpp
+++ b/cocos/platform/desktop/CCGLViewImpl-desktop.cpp
@@ -26,7 +26,11 @@ THE SOFTWARE.
 
 #include "platform/desktop/CCGLViewImpl-desktop.h"
 
+#include <chrono>
 #include <cmath>
+#include <cstdint>
+#include <cstdlib>
+#include <cstring>
 #include <unordered_map>
 
 #include "platform/CCApplication.h"
@@ -218,6 +222,26 @@ GLViewImpl::GLViewImpl(bool initglfw)
     if (initglfw)
     {
         glfwSetErrorCallback(GLFWEventHandler::onGLFWError);
+#if defined(ROCGALGAME_LINUX_GLES2)
+        const char* displayBackend =
+            std::getenv("ROCGALGAME_KRKR_DISPLAY_BACKEND");
+        const bool useWayland = displayBackend &&
+            std::strcmp(displayBackend, "wayland") == 0;
+        const bool useX11 = displayBackend &&
+            (std::strcmp(displayBackend, "x11") == 0 ||
+             std::strcmp(displayBackend, "xwayland") == 0);
+        const int platformHint =
+            useWayland ? GLFW_PLATFORM_WAYLAND :
+            (useX11 && glfwPlatformSupported(GLFW_PLATFORM_X11) ?
+                 GLFW_PLATFORM_X11 :
+                 GLFW_ANY_PLATFORM);
+        glfwInitHint(GLFW_PLATFORM, platformHint);
+        if (useWayland)
+            glfwInitHint(GLFW_WAYLAND_LIBDECOR, GLFW_WAYLAND_DISABLE_LIBDECOR);
+        log("[rocgalgame] GLFW platform hint: %s",
+            platformHint == GLFW_PLATFORM_WAYLAND ? "wayland" :
+            (platformHint == GLFW_PLATFORM_X11 ? "x11" : "auto"));
+#endif
         glfwInit();
     }
 }
@@ -282,6 +306,11 @@ bool GLViewImpl::initWithRect(const std::string& viewName, Rect rect, float fram
 {
     setViewName(viewName);
 
+#if defined(ROCGALGAME_LINUX_GLES2)
+    glfwWindowHint(GLFW_CLIENT_API, GLFW_OPENGL_ES_API);
+    glfwWindowHint(GLFW_CONTEXT_VERSION_MAJOR, 2);
+    glfwWindowHint(GLFW_CONTEXT_VERSION_MINOR, 0);
+#endif
     _frameZoomFactor = frameZoomFactor;
 
     glfwWindowHint(GLFW_RESIZABLE,resizable?GL_TRUE:GL_FALSE);
@@ -334,6 +363,18 @@ bool GLViewImpl::initWithRect(const std::string& viewName, Rect rect, float fram
     }
 
     glfwMakeContextCurrent(_mainWindow);
+#if defined(ROCGALGAME_LINUX_GLES2)
+    int swapInterval = 0;
+    if (const char* value = std::getenv("ROCGALGAME_KRKR_SWAP_INTERVAL"))
+    {
+        char* end = nullptr;
+        const long parsed = std::strtol(value, &end, 10);
+        if (end != value && *end == '\0' && (parsed == 0 || parsed == 1))
+            swapInterval = static_cast<int>(parsed);
+    }
+    glfwSwapInterval(swapInterval);
+    log("OpenGL ES swap interval: %d", swapInterval);
+#endif
 
     glfwSetMouseButtonCallback(_mainWindow, GLFWEventHandler::onGLFWMouseCallBack);
     glfwSetCursorPosCallback(_mainWindow, GLFWEventHandler::onGLFWMouseMoveCallBack);
@@ -351,6 +392,14 @@ bool GLViewImpl::initWithRect(const std::string& viewName, Rect rect, float fram
     // check OpenGL version at first
     const GLubyte* glVersion = glGetString(GL_VERSION);
 
+#if defined(ROCGALGAME_LINUX_GLES2)
+    const GLubyte* glRenderer = glGetString(GL_RENDERER);
+    log("OpenGL ES version: %s", glVersion ? (const char*)glVersion : "unknown");
+    log("OpenGL ES renderer: %s", glRenderer ? (const char*)glRenderer : "unknown");
+    log("[rocgalgame] GLFW context current: %s",
+        glfwGetCurrentContext() == _mainWindow ? "yes" : "no");
+    initGlew();
+#else
     if ( utils::atof((const char*)glVersion) < 1.5 )
     {
         char strComplain[256] = {0};
@@ -362,15 +411,22 @@ bool GLViewImpl::initWithRect(const std::string& viewName, Rect rect, float fram
     }
 
     initGlew();
+#endif
 
+#if !defined(ROCGALGAME_LINUX_GLES2)
     // Enable point size by default.
     glEnable(GL_VERTEX_PROGRAM_POINT_SIZE);
     
     if(_glContextAttrs.multisamplingCount > 0)
         glEnable(GL_MULTISAMPLE);
+#endif
 
 //    // GLFW v3.2 no longer emits "onGLFWWindowSizeFunCallback" at creation time. Force default viewport:
+#if defined(ROCGALGAME_LINUX_GLES2)
+    setViewPortInPoints(0, 0, rect.size.width, rect.size.height);
+#else
 //    setViewPortInPoints(0, 0, neededWidth, neededHeight);
+#endif
 //
     return true;
 }
@@ -421,7 +477,115 @@ void GLViewImpl::end()
 void GLViewImpl::swapBuffers()
 {
     if(_mainWindow)
-        glfwSwapBuffers(_mainWindow);
+    {
+#if defined(ROCGALGAME_LINUX_GLES2)
+        const auto started = std::chrono::steady_clock::now();
+        static unsigned int swapProbeCall = 0;
+        const char* swapProbe = std::getenv("ROCGALGAME_KRKR_SWAP_PROBE");
+        if (swapProbe && swapProbe[0] && std::strcmp(swapProbe, "0") != 0 &&
+            ++swapProbeCall <= 256 &&
+            (swapProbeCall & (swapProbeCall - 1)) == 0)
+        {
+            const GLenum pendingError = glGetError();
+            GLint framebuffer = 0;
+            GLint viewport[4] = {0, 0, 0, 0};
+            GLint program = 0;
+            GLint texture = 0;
+            glGetIntegerv(GL_FRAMEBUFFER_BINDING, &framebuffer);
+            glGetIntegerv(GL_VIEWPORT, viewport);
+            glGetIntegerv(GL_CURRENT_PROGRAM, &program);
+            glGetIntegerv(GL_TEXTURE_BINDING_2D, &texture);
+            const GLenum framebufferStatus =
+                glCheckFramebufferStatus(GL_FRAMEBUFFER);
+            const int xs[] = {viewport[0], viewport[0] + viewport[2] / 4,
+                              viewport[0] + viewport[2] / 2,
+                              viewport[0] + viewport[2] * 3 / 4,
+                              viewport[0] + viewport[2] - 1};
+            const int ys[] = {viewport[1], viewport[1] + viewport[3] / 4,
+                              viewport[1] + viewport[3] / 2,
+                              viewport[1] + viewport[3] * 3 / 4,
+                              viewport[1] + viewport[3] - 1};
+            std::uint32_t sampleOr = 0;
+            if (viewport[2] > 0 && viewport[3] > 0)
+            {
+                glPixelStorei(GL_PACK_ALIGNMENT, 4);
+                for (unsigned int i = 0; i < sizeof(xs) / sizeof(xs[0]); ++i)
+                {
+                    std::uint32_t pixel = 0;
+                    glReadPixels(xs[i], ys[i], 1, 1, GL_RGBA,
+                                 GL_UNSIGNED_BYTE, &pixel);
+                    sampleOr |= pixel;
+                }
+            }
+            const GLenum readError = glGetError();
+            log("[rocgalgame] swap_probe call=%u fbo=%d status=0x%04X "
+                "viewport=%d,%d,%d,%d program=%d texture=%d "
+                "pending=0x%04X read=0x%04X sample_or=0x%08X",
+                swapProbeCall, framebuffer, framebufferStatus, viewport[0],
+                viewport[1], viewport[2], viewport[3], program, texture,
+                pendingError, readError, sampleOr);
+            const char* eglProbe = std::getenv("ROCGALGAME_KRKR_EGL_PROBE");
+            if (eglProbe && eglProbe[0] && std::strcmp(eglProbe, "0") != 0)
+            {
+                EGLDisplay display = eglGetCurrentDisplay();
+                EGLContext context = eglGetCurrentContext();
+                EGLint configId = -1;
+                eglQueryContext(display, context, EGL_CONFIG_ID, &configId);
+                EGLConfig configs[256];
+                EGLint configCount = 0;
+                EGLConfig config = nullptr;
+                if (eglGetConfigs(display, configs, 256, &configCount))
+                {
+                    for (EGLint i = 0; i < configCount; ++i)
+                    {
+                        EGLint id = -1;
+                        eglGetConfigAttrib(display, configs[i], EGL_CONFIG_ID, &id);
+                        if (id == configId)
+                        {
+                            config = configs[i];
+                            break;
+                        }
+                    }
+                }
+                auto eglAttr = [display, config](EGLint attr) {
+                    EGLint value = -1;
+                    if (config) eglGetConfigAttrib(display, config, attr, &value);
+                    return value;
+                };
+                EGLSurface surface = eglGetCurrentSurface(EGL_DRAW);
+                EGLint width = -1;
+                EGLint height = -1;
+                eglQuerySurface(display, surface, EGL_WIDTH, &width);
+                eglQuerySurface(display, surface, EGL_HEIGHT, &height);
+                log("[rocgalgame] egl_probe call=%u config=%d rgba=%d/%d/%d/%d "
+                    "buffer=%d depth=%d stencil=%d native_visual=0x%X "
+                    "surface_type=0x%X renderable=0x%X surface=%dx%d",
+                    swapProbeCall, configId,
+                    eglAttr(EGL_RED_SIZE), eglAttr(EGL_GREEN_SIZE),
+                    eglAttr(EGL_BLUE_SIZE), eglAttr(EGL_ALPHA_SIZE),
+                    eglAttr(EGL_BUFFER_SIZE), eglAttr(EGL_DEPTH_SIZE),
+                    eglAttr(EGL_STENCIL_SIZE), eglAttr(EGL_NATIVE_VISUAL_ID),
+                    eglAttr(EGL_SURFACE_TYPE), eglAttr(EGL_RENDERABLE_TYPE),
+                    width, height);
+            }
+        }
+#endif
+        glfwSwapBuffers(_mainWindow);
+#if defined(ROCGALGAME_LINUX_GLES2)
+        const auto elapsed = std::chrono::duration_cast<std::chrono::milliseconds>(
+            std::chrono::steady_clock::now() - started).count();
+        static auto lastSlowSwapReport = std::chrono::steady_clock::time_point{};
+        const auto now = std::chrono::steady_clock::now();
+        if (elapsed >= 50 &&
+            (lastSlowSwapReport.time_since_epoch().count() == 0 ||
+             now - lastSlowSwapReport >= std::chrono::seconds(5)))
+        {
+            log("[rocgalgame] slow OpenGL ES swap: %lld ms",
+                static_cast<long long>(elapsed));
+            lastSlowSwapReport = now;
+        }
+#endif
+    }
 }
 
 bool GLViewImpl::windowShouldClose()
@@ -1022,6 +1186,9 @@ static bool glew_dynamic_binding()
 // helper
 bool GLViewImpl::initGlew()
 {
+#if defined(ROCGALGAME_LINUX_GLES2)
+    return true;
+#else
 #if (CC_TARGET_PLATFORM != CC_PLATFORM_MAC)
     GLenum GlewInitResult = glewInit();
     if (GLEW_OK != GlewInitResult)
@@ -1057,6 +1224,7 @@ bool GLViewImpl::initGlew()
 #endif
 
 #endif // (CC_TARGET_PLATFORM != CC_PLATFORM_MAC)
+#endif
 
     return true;
 }
diff --git a/cocos/platform/linux/CCDevice-linux.cpp b/cocos/platform/linux/CCDevice-linux.cpp
index 92e4137..b91b013 100644
--- a/cocos/platform/linux/CCDevice-linux.cpp
+++ b/cocos/platform/linux/CCDevice-linux.cpp
@@ -32,6 +32,7 @@ THE SOFTWARE.
 
 #include <X11/Xlib.h>
 #include <stdio.h>
+#include <cstdlib>
 
 #include <algorithm>
 #include <vector>
@@ -82,6 +83,20 @@ NS_CC_BEGIN
 
 int Device::getDPI()
 {
+#if defined(ROCGALGAME_LINUX_GLES2)
+    static int dpi = [] {
+        const char* value = std::getenv("ROCGALGAME_KRKR_DPI");
+        if (value)
+        {
+            char* end = nullptr;
+            const long parsed = std::strtol(value, &end, 10);
+            if (end != value && *end == '\0' && parsed >= 72 && parsed <= 1000)
+                return static_cast<int>(parsed);
+        }
+        return 160;
+    }();
+    return dpi;
+#else
     static int dpi = -1;
     if (dpi == -1)
     {
@@ -103,6 +118,7 @@ int Device::getDPI()
         XCloseDisplay (dpy);
     }
     return dpi;
+#endif
 }
 
 void Device::setAccelerometerEnabled(bool isEnabled)
diff --git a/cocos/platform/linux/CCGL-linux.h b/cocos/platform/linux/CCGL-linux.h
index 42c860b..be32d1c 100644
--- a/cocos/platform/linux/CCGL-linux.h
+++ b/cocos/platform/linux/CCGL-linux.h
@@ -29,7 +29,31 @@ THE SOFTWARE.
 #include "platform/CCPlatformConfig.h"
 #if CC_TARGET_PLATFORM == CC_PLATFORM_LINUX
 
+#if defined(ROCGALGAME_LINUX_GLES2)
+#define glClearDepth                glClearDepthf
+#define glDeleteVertexArrays        glDeleteVertexArraysOES
+#define glGenVertexArrays           glGenVertexArraysOES
+#define glBindVertexArray           glBindVertexArrayOES
+#define glMapBuffer                 glMapBufferOES
+#define glUnmapBuffer               glUnmapBufferOES
+#ifndef GL_GLEXT_PROTOTYPES
+#define GL_GLEXT_PROTOTYPES 1
+#endif
+#include <EGL/egl.h>
+#include <GLES2/gl2.h>
+#include <GLES2/gl2ext.h>
+#ifndef GL_DEPTH24_STENCIL8
+#define GL_DEPTH24_STENCIL8 0x88F0
+#endif
+#ifndef GL_WRITE_ONLY
+#define GL_WRITE_ONLY GL_WRITE_ONLY_OES
+#endif
+#ifndef GL_BGRA
+#define GL_BGRA GL_BGRA_EXT
+#endif
+#else
 #include "GL/glew.h"
+#endif
 
 #define CC_GL_DEPTH24_STENCIL8      GL_DEPTH24_STENCIL8
 
diff --git a/cocos/renderer/CCGLProgram.cpp b/cocos/renderer/CCGLProgram.cpp
index 04785f2..85d5c35 100644
--- a/cocos/renderer/CCGLProgram.cpp
+++ b/cocos/renderer/CCGLProgram.cpp
@@ -488,7 +488,7 @@ bool GLProgram::compileShader(GLuint * shader, GLenum type, const GLchar* source
         headersDef = (type == GL_VERTEX_SHADER ? "precision mediump float;\n precision mediump int;\n" : "precision mediump float;\n precision mediump int;\n");
 // Bugfix to make shader variables types constant to be understood by the current Android Virtual Devices or Emulators. This will also eliminate the 0x501 and 0x502 OpenGL Errors during emulation.
 // Changed shader data types mediump to highp to remove possible sprite joggling on some Android phones.
-#elif CC_TARGET_PLATFORM == CC_PLATFORM_ANDROID
+#elif CC_TARGET_PLATFORM == CC_PLATFORM_ANDROID || defined(ROCGALGAME_LINUX_GLES2)
         headersDef = "#version 100\n precision highp float;\n precision highp int;\n";
 #elif (CC_TARGET_PLATFORM != CC_PLATFORM_WIN32 && CC_TARGET_PLATFORM != CC_PLATFORM_LINUX && CC_TARGET_PLATFORM != CC_PLATFORM_MAC)
         headersDef = (type == GL_VERTEX_SHADER ? "precision highp float;\n precision highp int;\n" : "precision mediump float;\n precision mediump int;\n");
