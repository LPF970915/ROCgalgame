diff --git a/cpp/plugins/psbfile/main.cpp b/cpp/plugins/psbfile/main.cpp
index 8135328..289e558 100644
--- a/cpp/plugins/psbfile/main.cpp
+++ b/cpp/plugins/psbfile/main.cpp
@@ -20,7 +20,6 @@
 #define LOGGER spdlog::get("plugin")
 
 using namespace PSB;
-static PSBMedia *psbMedia = nullptr;
 
 void initPsbFile() { initPSBMedia(); }
 
@@ -53,20 +52,18 @@ static tjs_error load(tTJSVariant *r, tjs_int count, tTJSVariant **p,
 
     if((*p)->Type() == tvtString) {
         ttstr path{ **p };
-        if(!self->loadPSBFile(path)) {
+        if(self == nullptr || !self->loadPSBFile(path) ||
+           self->getObjects() == nullptr) {
             LOGGER->info("cannot load psb file : {}", path.AsStdString());
             loadSuccess = false;
+        } else {
+            // PSBMediaRegistry owns the registered media instance. The old
+            // file-local pointer was never initialized and crashed here.
+            registerRootResources(path, *self);
         }
-        auto objs = self->getObjects();
-        for(const auto &[k, v] : *objs) {
-            const auto &res = std::dynamic_pointer_cast<PSBResource>(v);
-            if(res == nullptr)
-                continue;
-            ttstr pathN{ k };
-            psbMedia->NormalizeDomainName(path);
-            psbMedia->NormalizePathName(pathN);
-            psbMedia->add((path + TJS_W("/") + pathN).AsStdString(), res);
-        }
     } else if((*p)->Type() == tvtOctet) {
         LOGGER->critical("PSBFile::load stream no implement!");
         loadSuccess = false;
@@ -124,7 +121,10 @@ static tjs_error PSBFileFactory(PSBFile **result, tjs_int count,
     } else if(count == 1 && (*params)->Type() == tvtString) {
         ttstr path{ *params[0] };
         psbFile = new PSBFile();
-        psbFile->loadPSBFile(path);
+        if(psbFile->loadPSBFile(path) && psbFile->getObjects() != nullptr) {
+            registerRootResources(path, *psbFile);
+        }
     } else {
         return TJS_E_INVALIDPARAM;
     }
