diff --git a/cpp/core/base/StorageIntf.cpp b/cpp/core/base/StorageIntf.cpp
index e0f1f66..fc03e5f 100644
--- a/cpp/core/base/StorageIntf.cpp
+++ b/cpp/core/base/StorageIntf.cpp
@@ -1062,6 +1062,8 @@ ttstr TVPGetPlacedPath(const ttstr &name) {
     // search path and return the path which the "name" is placed.
     // returned name is normalized. returns empty string if the
     // storage is not found.
+    if(name.IsEmpty())
+        return {};
 #if 0 // needn't
     if(!TVPClearAutoPathCacheCallbackInit)
     {
diff --git a/cpp/core/tjs2/tjsInterCodeExec.cpp b/cpp/core/tjs2/tjsInterCodeExec.cpp
index c3e7a72..a53efc5 100644
--- a/cpp/core/tjs2/tjsInterCodeExec.cpp
+++ b/cpp/core/tjs2/tjsInterCodeExec.cpp
@@ -47,6 +47,13 @@ namespace TJS {
     static void GetStringProperty(tTJSVariant *result, const tTJSVariant *str,
                                   const tTJSVariant &member) {
         // processes properties toward strings.
+        // Empty TJS strings are represented by a null string object. Keep
+        // property access well-defined instead of calling through that null
+        // object and turning a script error into a native crash.
+        const tTJSVariantString *value = str->AsStringNoAddRef();
+        const tjs_int length = value ? value->GetLength() : 0;
+        const tjs_char *chars = value ? static_cast<const tjs_char *>(*value)
+                                      : TJS_W("");
         if(member.Type() != tvtInteger && member.Type() != tvtReal) {
             const tjs_char *name = member.GetString();
             if(!name)
@@ -54,24 +61,20 @@ namespace TJS {
 
             if(!TJS_strcmp(name, TJS_W("length"))) {
                 // get string length
-                const tTJSVariantString *s = str->AsStringNoAddRef();
-                *result = tTVInteger(s->GetLength());
+                *result = tTVInteger(length);
                 return;
             }
             if(name[0] >= TJS_W('0') && name[0] <= TJS_W('9')) {
-                const tTJSVariantString *valstr = str->AsStringNoAddRef();
-                const tjs_char *s = str->GetString();
                 tjs_int n = TJS_atoi(name);
-                tjs_int len = valstr->GetLength();
-                if(n == len) {
+                if(n == length) {
                     *result = tTJSVariant(TJS_W(""));
                     return;
                 }
-                if(n < 0 || n > len)
+                if(n < 0 || n > length)
                     TJS_eTJSError(TJSRangeError);
                 tjs_char bf[2];
                 bf[1] = 0;
-                bf[0] = s[n];
+                bf[0] = chars[n];
                 *result = tTJSVariant(bf);
                 return;
             }
@@ -80,19 +83,16 @@ namespace TJS {
         } else // member.Type() == tvtInteger || member.Type() ==
                // tvtReal
         {
-            const tTJSVariantString *valstr = str->AsStringNoAddRef();
-            const tjs_char *s = str->GetString();
             tjs_int n = (tjs_int)member.AsInteger();
-            tjs_int len = valstr->GetLength();
-            if(n == len) {
+            if(n == length) {
                 *result = tTJSVariant(TJS_W(""));
                 return;
             }
-            if(n < 0 || n > len)
+            if(n < 0 || n > length)
                 TJS_eTJSError(TJSRangeError);
             tjs_char bf[2];
             bf[1] = 0;
-            bf[0] = s[n];
+            bf[0] = chars[n];
             *result = tTJSVariant(bf);
         }
     }
diff --git a/cpp/core/tjs2/tjsString.h b/cpp/core/tjs2/tjsString.h
index 728744f..0871c9e 100644
--- a/cpp/core/tjs2/tjsString.h
+++ b/cpp/core/tjs2/tjsString.h
@@ -413,7 +413,9 @@ namespace TJS {
             return InternalIndepend();
         }
 
-        [[nodiscard]] tjs_int GetLen() const { return Ptr->GetLength(); }
+        [[nodiscard]] tjs_int GetLen() const {
+            return Ptr ? Ptr->GetLength() : 0;
+        }
 
         [[nodiscard]] tjs_int length() const { return GetLen(); }
 
diff --git a/cpp/core/tjs2/tjsVariantString.cpp b/cpp/core/tjs2/tjsVariantString.cpp
index f02757e..3f7c708 100644
--- a/cpp/core/tjs2/tjsVariantString.cpp
+++ b/cpp/core/tjs2/tjsVariantString.cpp
@@ -16,6 +16,13 @@
 #include "tjsLex.h"
 #include <algorithm>
 
+// Empty TJS strings are represented by a null string object. Several legacy
+// ABI methods intentionally accept a null `this`; GCC otherwise removes those
+// checks at -O3 and turns an empty script string into a native crash.
+#if defined(__GNUC__) && !defined(__clang__)
+#pragma GCC optimize("no-delete-null-pointer-checks")
+#endif
+
 namespace TJS {
     //---------------------------------------------------------------------------
     // throwing error functions
@@ -439,12 +446,10 @@ namespace TJS {
     // tTJSVariantString
     //---------------------------------------------------------------------------
     void tTJSVariantString::Release() {
-        /*
-                if(!this) return;
-                        // this is not a REAL remedy, but enough to
-           buster careless misses...
-                        // (nullptr->Release() problem)
-        */
+        // Empty variants may carry a null string object. Legacy TJS call sites
+        // release that representation directly, so treat it as a no-op.
+        if(!this)
+            return;
 
         if(RefCount == 0) {
             TJSDeallocStringHeap(this);
