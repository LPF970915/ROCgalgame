diff --git a/cpp/core/environ/cocos2d/AppDelegate.cpp b/cpp/core/environ/cocos2d/AppDelegate.cpp
--- a/cpp/core/environ/cocos2d/AppDelegate.cpp
+++ b/cpp/core/environ/cocos2d/AppDelegate.cpp
@@ -35,8 +35,6 @@ std::string TVPGetCurrentLanguage();
 
 void TVPAppDelegate::applicationWillEnterForeground() {
     ::Application->OnActivate();
-    if(TVPMainScene::GetInstance())
-        TVPMainScene::GetInstance()->resetRocgalgamePointerClock();
     cocos2d::Director::getInstance()->startAnimation();
 }
 
diff --git a/cpp/core/environ/cocos2d/MainScene.cpp b/cpp/core/environ/cocos2d/MainScene.cpp
--- a/cpp/core/environ/cocos2d/MainScene.cpp
+++ b/cpp/core/environ/cocos2d/MainScene.cpp
@@ -820,14 +820,9 @@ tjs_uint TVPGetGraphicCacheTotalBytes();
 void TVPMainScene::update(float delta) {
     ::Application->Run();
     consumeRocgalgameInputBridge();
-    const auto now = std::chrono::steady_clock::now();
-    float frameDelta = 0.0f;
-    if(_rocgalgamePointerClockValid) {
-        frameDelta = std::chrono::duration<float>(
-            now - _rocgalgamePointerTick).count();
-    }
-    _rocgalgamePointerTick = now;
-    _rocgalgamePointerClockValid = true;
+    const float frameDelta = std::isfinite(delta) && delta > 0.0f
+        ? std::min(delta, 0.1f) : 0.0f;
     if(!_rocgalgameBridgeActive)
         _virtualMouseAxisX = _virtualMouseAxisY = 0.0f;
     if(_virtualMouseEnabled &&
@@ -1139,10 +1134,6 @@ TVPMainScene::TVPMainScene() {
 
 TVPMainScene::~TVPMainScene() = default;
 
-void TVPMainScene::resetRocgalgamePointerClock() {
-    _rocgalgamePointerClockValid = false;
-}
-
 void TVPMainScene::showWindowManagerOverlay(bool bVisible) {
     if(bVisible) {
         if(!TVPWindowLayer::_windowMgrOverlay) {
diff --git a/cpp/core/environ/cocos2d/MainScene.h b/cpp/core/environ/cocos2d/MainScene.h
--- a/cpp/core/environ/cocos2d/MainScene.h
+++ b/cpp/core/environ/cocos2d/MainScene.h
@@ -5,7 +5,6 @@
 #include "base/CCIMEDelegate.h"
 
 #include <array>
-#include <chrono>
 #include <cstdint>
 #include <memory>
 #include <string>
@@ -69,7 +68,6 @@ public:
     static void onTextInput(const std::string &text);
 
     static float convertCursorScale(float val /*0 ~ 1*/);
-    void resetRocgalgamePointerClock();
 
 private:
     void onKeyPressed(cocos2d::EventKeyboard::KeyCode keyCode,
@@ -117,8 +115,6 @@ private:
     float _virtualMouseAxisY = 0.0f;
     float _virtualMouseSpeed = 1080.0f, _virtualMouseAccel = 1.0f;
     std::unique_ptr<RocgalgameInputTransport> _rocgalgameTransport;
-    std::chrono::steady_clock::time_point _rocgalgamePointerTick{};
-    bool _rocgalgamePointerClockValid = false;
     std::array<bool, 2> _rocgalgameMouseButtons{};
     std::array<bool, 0x200> _rocgalgameKeys{};
 };
