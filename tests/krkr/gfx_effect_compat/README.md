# gfxEffect compatibility fixture

This legacy KAG project exercises the native `gfxEffect.dll` compatibility
implementation carried by the KRKR2 port. The original Windows engine binary
is intentionally excluded; launch the project directory with the staged KRKR2
core during a device compatibility pass.
